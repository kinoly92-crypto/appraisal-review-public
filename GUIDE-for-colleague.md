# 给同事的使用指南（请先读）

这份技能包让你（在自己电脑、用你的 AI 助手）按统一标准审核估价报告，产出和原作者**格式一致**的三件套（批注版报告 / 审核意见 / 交叉验证报告）。

## 一、先装好环境（一次性）
1. 安装 **Python 3**。
2. 装依赖：命令行运行 `pip install openpyxl matplotlib`。
3. 确认有**中文字体 SimHei（黑体）**：Windows 自带（`C:\Windows\Fonts\simhei.ttf`）。Mac/Linux 没有的话，装一个中文字体并改 `scripts/build_crossvalidation.py` 顶部的 `FONT` 路径——否则图表中文会变方块、和原作者不一致。
4. 你的 AI 助手需要能**读写本地文件**并能**运行 Python**（如 Claude Code、或任何支持代码执行+文件的 Agent）。纯聊天、不能跑代码的 AI 做不到一致。

## 二、每次审核怎么做
1. 把要审的报告资料放进一个文件夹。
2. 把**整个技能包文件夹**提供给你的 AI，并发给它下面【三、开场提示词】那段话。
3. AI 会：先问你**本次审核人/批注人姓名** → 读资料 → 按规则审 → 用 `templates/`+`scripts/` 出三件套。
4. 你核对三件套，交付。

## 三、开场提示词（复制这段发给你的 AI）

```
你将使用「估价报告质控审核技能包」审核一份估价报告。请严格按以下步骤：
1. 先完整读技能包根目录的 SKILL.md，再读 rules/ 下与本报告类型对应的要点文件。
2. 第一步先用普通文字问我：本次的「审核人姓名」「批注人姓名」「审核日期（默认今天）」（不要假设默认值）；问完停下来等我回答，我回答后再继续。用纯文字问答即可，不要用特殊弹窗/表单，我没回答前不要读资料、不要出成果。
3. 读取我指定的报告资料（报告/说明/测算表/权属证等）；老式.doc读不出中文就让我转成.docx，扫描PDF无文字层就让我提供可读版或口述关键信息。务必先核测算表再对问题定性。
4. 严格套用 templates/ 里的三个docx + scripts/ 里的脚本产出三件套（批注版报告、审核意见、交叉验证报告），不要自由发挥另起格式：
   - 审核意见：逐条、Word自动连续编号、按文档大类分组、每大类首条前加类别提示；
   - 批注版：Word真批注、直接描述问题不加[重大]等前缀、作者用批注人姓名；
   - 交叉验证：数据对比表+两张图表+数据来源做成可点击超链接、多来源、搜不到如实说不编造；**若报告不涉及估值（无价值数额结论，多见于其他咨询类），则不做交叉验证，只出批注版+审核意见两件套**（见 rules/other-consulting.md）。
5. 产出后自检：编号连续、姓名/项目号正确、无其他项目残留、能在Word正常打开、链接可点击。
技能包路径：<把文件夹拖进来或填路径>
要审的报告：<报告文件路径>
```

## 四、为什么这样能"和原作者一致"
- 三件套的**格式、结构、编号方式、排版**由 `templates/`+`scripts/` 钉死——只要用它们，就一致。
- **但请注意**：具体写了哪些问题、措辞，取决于 AI 对**这份具体报告**的分析，不同报告本就不同；不同 AI 之间用词也会有差异。能保证**完全一致的是"形式/规范"，内容质量取决于是否严格遵守 rules/**。要内容也尽量贴近，就让 AI 严格按 `rules/` 的条款和检查项来，别跳步。

## 五、有疑问 / 规则要改
- 规则以 `rules/*.md` 为准；格式以 `templates/*.docx` 为准。
- 如需修改规则或模板，请联系原作者统一维护（建议用 git 版本管理），改完把**新版整包**再发一次，确保大家用的是同一版。

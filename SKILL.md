# 估价报告质控审核技能（SKILL.md）

> 本技能用于对评估/估价报告做**三级质控复核**，并产出**三件套交付物**。本文件是总入口：任何 AI 助手在执行审核前**必须先完整读本文件**，再读 `rules/` 下对应报告类型的要点。成果格式由 `templates/` + `scripts/` 钉死，务必复用，**不要自由发挥另起格式**。

## 〇、两种模式（开工前先判断走哪条）
本技能有**两种互斥的工作模式**，依据体系、定性口径、成果形式都不同：

| | **常规审核模式（默认）** | **执业质量检查模式** |
|---|---|---|
| 触发 | 用户只说"审一下""帮我审这个报告/项目" | 用户明确说 **"执行资产评估执业质量检查""按执业质量检查审""联合检查/自律检查视角""按检查手册审""出一事一议/检查问题汇总表"** 等 |
| 依据 | 本 SKILL.md + `rules/<报告类型>.md` | **`rules/quality-inspection.md`**（+ 报告类型规则叠加） |
| 时点 | 报告出具**前**（电子稿） | **同样是出具前电子稿**——第一节"文号/签章不作为问题提"的规则**两种模式都适用** |
| 范围 | 报告+说明+测算表/明细表 | 再加**工作底稿与档案**、委托合同、内部复核记录（如已提供） |
| 意见写法 | 只描述问题，不写依据条款、不写整改建议 | **必须写违反的准则/法规具体条款**，四段式"问题标题—问题描述—错误原因—造成后果（金额·差异率）" |
| 成果 | 三件套（见第三节） | **检查四件套**（见 `quality-inspection.md` §九） |

**判不准就用一句话问用户："本次按常规质控复核审，还是按执业质量检查（联合检查/自律检查）口径审？"**，等回答再开工。

## 一、适用与角色
- 使用者：评估机构质控部门（三级复核为最终把关；报告经其通过才正式出具）。
- **审核时点＝报告正式出具之前**：此刻报告尚未签发，**报告文号也尚未生成**（须本次审核通过后才出报告、才编文号）。因此**"报告/评估说明缺报告文号""无文号""文号待补"之类一律不作为审核问题提出**，形式要件审核中**跳过"报告文号"这一项**（其余形式要件照常审）。注：依据文件/法规所引的名称文号仍须核，与"报告自身文号"是两回事。
- **签字盖章同理（审核后才办）**：评估机构法定代表人签字、评估师/估价师签字、机构盖章/印章、骑缝章等均是**本次审核通过、报告正式出具时才完成的动作**。因此**"缺法定代表人签字""未见评估师签字""缺机构公章/盖章""未签字盖章""签章待补"之类一律不作为审核问题提出**，形式要件审核中**跳过"签字盖章"这一项**。注：仍须核的是**署名/签章的资格与一致性规则本身**——例如承办须≥2名相应专业类别评估师、署名人须与执业登记/备案名录一致、不得以个人名章代签字、不得以专用章代机构公章等"应当如何签章"的要求；这与"此刻尚未签字盖章"是两回事，前者照核、后者不提。
- **税的计算一律对照"最新税法"审核（所有项目通用）**：凡报告/评估说明/测算表中涉及税的测算（增值税及附加、土地增值税、契税、印花税、企业所得税、个人所得税、房产税、城镇土地使用税、资源税、耕地占用税、车辆购置税等），**不凭记忆、不沿用报告自带口径**，须从**公开权威资讯查证现行有效的税种/税目/税率/计税依据/优惠减免/征收方式**后再核（税法时效性强、近年频改）。查证以官方源为准：国家税务总局 chinatax.gov.cn、财政部 mof.gov.cn、国家法律法规数据库 flk.npc.gov.cn、以及省级税务局/地方政府对地方性税种(如资源税具体税率、契税适用税率、土地增值税预征率)的现行规定（查询路径见 `rules/data-sites.md` 财税·税法类）。审核要点：①所用税率/税目与现行法一致（注意过渡期、新旧法衔接）；②计税依据/扣除项正确（如土增税扣除、增值税进项、所得税前扣除）；③优惠/减免适用条件成立且在有效期内（不得擅自套用已废止或不适用的优惠）；④与评估目的/经济行为匹配（涉税评估尤其）；⑤报告所引税法名称文号为现行有效版本。**发现用了过时税率/已废止优惠/计税依据错=技术问题，按影响程度定级（可核实且影响评估值≥20% 属重大差错，见 asset-appraisal.md 重大遗漏框架）。**
- **⭐追溯性评估：新旧规范适用规则（所有报告类型通用）**——凡价值时点/评估基准日**明显早于报告出具日**的追溯性评估（已灭失财产索赔、追溯损失、历史时点重估、司法追溯定价等），依据的新旧适用按**"实体从旧、程序从新"**把握。来源：最高人民法院《关于审理行政案件适用法律规范问题的座谈会纪要》第三部分"关于新旧法律规范的适用规则"——行政相对人的行为发生在新法施行以前、具体行政行为作出在新法施行以后的，人民法院审查其合法性时，**实体问题适用旧法规定，程序问题适用新法规定**。落到评估上：
  - **"实体"＝具体测算过程中影响评估结论的取价依据** → 取**价值时点当时有效**的数据、价格、参数、税率、政策标准，并按价值时点的市场状况分析。
  - **"程序"＝报告出具日规范评估活动的有效准则** → 评估准则、执业规范、报告体例、程序要求一律按**报告出具日现行有效**的版本执行。
  - 一句话记法：**只在追溯的基础上考虑价值时点有效的数据及对数据的分析，而不是执行当时有效的评估准则。**
  - **审核抓手**：①报告是否混用——用价值时点当年的旧准则编报告（错）、或用报告日的现行价格去测追溯价值（错）；②取价依据是否表征价值时点（询价/案例/指数的时点须落在价值时点，见 `rules/asset-appraisal.md` 追溯评估专项）；③"评估依据"章节的准则版本应为出具日现行有效版本，取价类依据则应为价值时点版本，两类不可一锅烩；④若报告自行论述新旧适用而与上述原则相悖，按技术问题提出。
- **不再作为审核问题提出：评估说明"第二部分 关于进行资产评估有关事项的说明"由委托人/管理人及产权持有人撰写这一事项**（该部分内容形如"此部分由管理人及产权持有人撰写，具体内容详见附件"）。该部分已有**单独的线下审核**，本技能的审核意见与批注**一律不提**（既不提其撰写主体、也不提其内容缺失/附件未见/未签章）。注：其他部分引用该说明中的数据与报告正文矛盾时，仍按勾稽一致性照常提出，与本条不冲突。
- 适用报告类型（各有专属审核要点，见 `rules/`）：
  1. **资产评估报告**（固定资产/无形资产/企业价值/房地产年租金等）→ `rules/asset-appraisal.md`
     - **集团企业价值/股权（母公司口径、长投/其他权益含多家被投资公司、国企改制/混改/抽查自查）→ 在 asset-appraisal.md 之上叠加 `rules/enterprise-value-group.md`**（对应关系图先行·三段式流程·逐户跨项目勾稽·测算底稿必审）
  2. **房地产估价报告** → `rules/realestate-appraisal.md`
  3. **土地估价报告** → `rules/land-appraisal.md`
  4. **债权分析报告**（金融不良资产·债权价值分析/价值分析业务）→ `rules/debt-analysis.md`
  5. **矿业权评估报告**（探矿权/采矿权·出让价款/转让/抵押/司法/上市/改制）→ `rules/mining-rights.md`
  6. **其他咨询类报告**（估值分析/价值分析/债权价值分析[非不良]等·形式多样无专门准则，参照公开权威资讯审核）→ `rules/other-consulting.md`（**涉及估值的需交叉验证，无估值的不做交叉验证**）
- **跨模式叠加：执业质量检查（财政部+中评协联合检查／协会自律检查／机构自查）** → `rules/quality-inspection.md`（依据《资产评估行业执业质量检查工作手册（2026年）》+《中国资产评估检查法规汇编（2026年）》；含五类检查底稿框架、一事一议问题定性定量规则、准则条款选取顺序、问题分类字典、机构内部治理与质控体系检查要点、自律惩戒与行政处罚定级映射）
- **跨类型叠加：司法涉执（人民法院委托财产处置参考价）评估** → `rules/judicial-disposal.md`（资产/房地产/土地/矿业权/珠宝的司法处置项目，在对应类型规则之上叠加此共性要点：不考虑查封担保/不考虑处置费用/市场价值/30日+有效期≤1年/法院审查5情形/异议与专业技术评审/名单库资质/附件材料清单）
- 通用审核方法与"依据+公开资源+迭代"原则：`rules/method-and-iteration.md`
- 公开数据查询优先网站：`rules/data-sites.md`
- **交付物格式规则（最重要，三类报告通用）**：`rules/deliverable-format.md`
- **成品格式基线**：本包**不再随附成品样例**（原 `examples/` 因含真实客户信息已于 2026-08-13 删除）。交付物格式**不靠模仿样例，靠 `templates/` + `scripts/` 强制**：审核意见跑 `build_opinion.py`（自带 7 类强制自检），格式规格见 `rules/deliverable-format.md`。需要回归比对时，用 `rules/deliverable-format.md §2.3` 指定的基线文件。**严禁把真实项目成品放回本仓库**（本仓库有公开远程）。

## 二、标准流程（每次审核照此走）
1. **第一步：问询本次「审核人姓名」「批注人姓名」**（二者可不同，可相同）。**不要假设默认值。** 同时确认审核日期（默认当天）。这两个名字将写入：审核意见信息表「审核人」、批注版 Word 批注的作者(author/initials)。
   - **跨平台执行方式（重要）**：用**普通对话文字**直接提问（例如："开始前请告知：本次审核人姓名、批注人姓名、审核日期[默认今天]？"），然后**停下来等用户回复**，收到回答后再继续后续步骤。**不要**依赖任何特定弹窗/表单/专用工具（不同 AI 平台不一定支持，避免卡住或报错）；**不要**自行假设、编造或跳过；用户未回答前不要读资料、不要生成任何成果。若平台支持结构化提问控件可用，但**纯文本问答必须始终可行**，这是保证换平台不卡运行的底线。
2. **清点并读取资料**：报告正文 + 评估说明（资产评估才有）+ 测算表/明细表 + 权属证明（不动产证等）+ 委托合同/附件。判断报告类型与（房地产/土地）业务类型（抵押/转让/征收/司法/出让…），据此选 `rules/` 对应要点 + 专项依据。
   - **⭐先看评估基准日定"程序准则"版本（资产评估/企业价值/矿业权等资产评估准则体系项目）**：《资产评估执业准则——资产评估程序》新版**2027-01-01 施行、废止中评协〔2018〕36号**。**基准日 ≥ 2027-01-01 的项目按新版审核**（基本程序 8项→9项、内部审核+业务报备为独立强制程序、程序受限"无法判断影响程度不得出报告"、业务报备前置且与报备系统一致、评估假设独立披露等），详见 `rules/asset-appraisal.md §一B`；基准日在 2027 前的仍按旧版。
   - **测算表/明细表（Excel）只审"非隐藏"的 sheet**：隐藏/极隐藏（hidden/veryHidden）的工作表**一律不审、不提意见**。`extract.py` 已自动跳过隐藏 sheet；若用其他方式读表，也只看可见 sheet。
3. **比对审核**：以"准则/规程为核心 + 公开网络资料补充印证"（见 `rules/method-and-iteration.md`），逐项核对。**重点：报告↔说明↔测算表↔权属证三方/四方勾稽一致性；数值复算；方法选用与适用性；价值时点；披露充分性；模板残留/前后矛盾。涉及税的测算，一律按现行最新税法从公开权威源查证后复核（见第一节"税的计算"总规则）。**
   - **⭐逐表逐列常识/量纲校验（不止结论链路）**：测算表/明细表/**申报表**等每一张可见 sheet 的**每一列**都要过一遍最朴素的常识与量纲检查——楼层应是个位/小数层数、竣工日期应是日期、面积/年限/比例应在合理量级、单价/金额量纲正确。**勿只盯"结论来源"那几张表而把申报表等降级略读。**（教训：某资产评估项目「评估申报明细表」因与「评估明细表」列顺序不同、数据按原顺序填入致"竣工日期↔楼层"两列错位，楼层列显示为日期序列号 44370/45119… 即 2021-06-23/2023-07-12，曾被漏审——见 `asset-appraisal.md §八`。）
   - **⭐同一字段跨表交叉比对**：同一字段（楼层/竣工日期/面积/结构/产权证号/坐落…）在报告、说明、评估明细表、评估申报明细表、因素调整系数表中的取值须逐一对齐；**不得因主表正确就默认其余表一致**；出现裸序列号、错列、缺位、漏字（如证号"002190"vs"0021920"）即应定位为问题。
4. **公开数据交叉验证**：按 `rules/data-sites.md` 优先查证估价值合理性（抵押/转让尤其不得高估）。搜不到就**如实说明、绝不编造**；多来源、按用途/区位/容积率/业态/楼层匹配可比。**取数只取相关、非隐藏数据**：报告取值/参数取自报告正文、评估说明、非隐藏 sheet；隐藏 sheet 及与评估对象/价值时点无关的数据不取、不入对比表。
   - **⭐交叉验证的"第三方/印证数据"必须来自独立第三方公开来源**（中介挂牌/成交平台、政府/行业公布价、公开招租拍卖公告等），**不得用本评估项目自身的可比案例价/询价记录/测算表取数充当第三方数据**——那是报告内部取数，用它"印证"自己等于自证，无验证意义。**独立第三方数据找不到就如实写"未找到/找不到"**，绝不编造、也不得拿项目自带案例价顶替（项目自带案例价只可放在"报告取值"一侧，不可放在"第三方/印证数据"一侧）。
5. **【第一批交付】先出批注版 + 交叉验证报告**（见第三节 + `rules/deliverable-format.md`）：批注版报告（资产评估还有说明）＋交叉验证分析报告。**此时不要生成审核意见文档。**
6. **⭐停下来等人工介入审核**：交付第一批后，用**普通对话文字**问一句——**"审核意见是否明确？（请在批注版里直接增删改批注，改完告诉我）"**，然后**停止，等用户回答**。用户会在 Word 里亲自调整批注（删掉不认可的、改写措辞、补新问题）。**未得到用户明确答复前，不得生成审核意见文档。** 跨平台执行方式同第 1 步（纯文字问答，不依赖弹窗/表单）。
7. **【第二批交付】人工确认后再出审核意见**：以**用户改完后的批注版 docx 当下的批注实况**为准（不是我先前列的清单、不是记忆里的版本），跑 `scripts/build_opinion_from_annotated.py` **回读批注** → 合并评估明细表/测算表等 Excel 类意见（这些无 Word 批注，由 `EXTRA_GROUPS` 手工给）→ 生成套 v2 模板的独立审核意见 docx。
   - **序号不会断号**：成品序号是 **Word 自动编号**（在 Word 里直接删条即自动重排）；脚本侧生成时也现场 1..N（人工删批注或用 `DROP` 剔条后重跑）。**永远不必、也不要手打编号。**
   - 若用户答"不明确/还要再看"，就停在第 6 步等，**别抢跑**。
   - 用户如果改完批注版另存了新文件名，**必须用新文件**回读；拿旧文件回读＝交付与批注版对不上。
8. 交付前自检：编号是否连续、信息表姓名/项目号是否正确、无他项目残留（他地区名、他业务类型词等）、XML 合法、链接可点击；**审核意见条目与批注版现存批注逐条对得上**（条数、措辞一致）。

## 三、三件套交付物（形式严格一致；详规见 `rules/deliverable-format.md`）

> **⭐交付顺序＝两批，中间夹一道人工介入（2026-08-25 起）**：
> **第一批**＝批注版（报告／说明）＋交叉验证分析报告 → **停下问"审核意见是否明确？"并等用户回答** → 用户在 Word 里增删改批注 → **第二批**＝按**改完后的批注实况**回读生成审核意见 docx（`build_opinion_from_annotated.py`），序号现场 1..N 重排。
> **审核意见的唯一事实来源＝当下批注版里真实存在的批注 + 表类（明细表/测算表）意见**，不是模型先前口头列的清单。


1. **批注版报告**（Word 真批注）：在报告文档上逐条加批注，**直接描述问题、不加[重大]/[一般]/[提示]等等级前缀**，**只描述问题，不写依据条款、不写整改建议**。
   - 资产评估：报告 + 评估说明各出一份批注版；房地产/土地：通常单一文档，出一份。
   - 测算表/明细表是 Excel，不做 Word 批注，其意见进"审核意见"。
   - 批注作者 = 本次问询的"批注人姓名"。
2. **审核意见**（独立 Word 文档，套 `templates/opinion-template.docx` —— **v2 模板，2026-08 起启用，仅用于三级审核**；**在人工确认批注后才生成**，见上方交付顺序）：
   - 标题**分两行**：第1行 `对《<完整项目名>》`、第2行 `三级审核意见及回复记录`（**模板自带两段，脚本只替换占位符，勿自行拆段**）+ 右上 `索引号：G-18-3` + 信息表（项目编号/二级复核人/**审核人=问询所得**/审核日期/回复人/**回复日期**）+ 正文表 + **文末 3×3 签字表（生成时留空）**。
   - 逐条块：`N、`／`位置：`／`原文：`／`审核意见：`(标签独占一行，正文另起段缩进2字符；只描述问题，不写依据条款、不写整改建议)／`回复：`(留空，带下边框)／空段(带下边框)／空段(无边框，**全篇最后一条省去**)。
   - **序号＝Word 真·自动编号（`N、`，跨大类连续）**：v2 模板本身不带 numbering.xml，由 `build_opinion.py` 生成时注入编号部件（numbering.xml + Content_Types + rels），序号段是空文本、数字由 Word 渲染。**两条路都不会断号**：①在 Word 里直接删掉整条意见，后续序号自动重排；②改配置重跑脚本，同样现场 1..N。**任何情况下都不要手打序号。**（2026-08-25 用户要求；已用 Word COM 实测：34 条删第5条后为 1..33 连续。）
   - **按文档大类分组排序**（资产评估报告→评估说明→评估明细表→评估测算表；房地产/土地：报告→测算表），每大类首条前加加粗类别提示行（如"资产评估报告："）。**无意见的大类不输出。** 序号跨类连续。
   - ⚠️ **必须跑 `scripts/build_opinion.py` 生成，禁止手写 XML / 自由排版**；脚本自检不过会 `[FAIL]` 退出，别绕过。
3. **交叉验证分析报告**（套 `templates/crossvalidation-template.docx`）：标题+副标题(项目名+编号) + 一、数据对比表(5列) + 二、可视化(图1/图2，matplotlib柱状图嵌入) + 三、分析说明(【数据一致性核查】+【数据来源说明】) + 四、验证结论。**数据来源 URL 必须做成可点击超链接。** 多来源、如实。

## 四、构建脚本（在 `scripts/`，复用以保证一致）
- `extract.py`：读取 docx/xlsx 文本、探测 PDF 文字层、UTF-16 启发式读老 .doc。**.xlsx 无 openpyxl 也能读**（自动纯标准库降级），不必为读测算表先装包。
- `build_opinion.py`：克隆审核意见 v2 模板（标题/索引号/信息表/正文表/签字表）+ 分组 + 逐条块 + 强制自检。键：PROJECT_NAME/PROJECT_NO/REVIEWER/REVIEW_DATE/SECOND_REVIEWER/REPLIER/REPLY_DATE/OUTPUT/GROUPS。
- `build_opinion_from_annotated.py`：**（新流程主力）** 从**人工调整后的批注版 docx** 回读现存批注（按文档顺序）→ 合并 `EXTRA_GROUPS` 里的明细表/测算表意见 → 调 `build_opinion.py` 出审核意见。自动**滤掉非本次批注人的批注**（`AUTHORS`，如承做人旧批注）、**跳过 Word 里标"已解决"的批注**、**跳过回复型子批注**（可配 `REPLY_MODE`）；`--preview` 只列清单不产文件；`DROP` 可再剔任意条，**序号一律现场 1..N 重排**。
- `build_annotated.py`：从零给 docx 注入 Word 真批注（comments.xml + 锚点 + rels + 内容类型）。锚点匹配已**自动归一化**（去全角/半角空白、反转义 `&<>`），不易失配；失配会打印候选段落。
- `build_crossvalidation.py`：matplotlib 出图 + 克隆交叉验证模板 + 可点击链接。
- `check_env.py`：**每个会话开头跑一次**，确认依赖；缺啥才按它给的唯一一行 `pip install`，**装一次别重复装**。
- **两种填数据方式**（任选其一）：①改脚本顶部"【按项目填写】"区后 `python build_xxx.py`；②（**跨平台首选**）用 **Write 工具**写一个 UTF-8 的 JSON 配置，再 `python build_xxx.py 配置.json`——键名同脚本里的大写变量。详见 `scripts/USAGE.md`。

## 五、环境要求 & 已知坑
- 需要：能读写文件、能跑 **Python**。docx/xlsx 直接用 `zipfile`+XML 处理最稳。
- **依赖最小化、装一次别重复（提速关键）**：开会话先 `python scripts/check_env.py` 自检；**依赖在同一会话内持久，缺了才装、且只装一次，切勿每步重装**。`openpyxl` 仅为读 .xlsx 保真（缺失自动纯标准库降级，仍可读）；`matplotlib`+中文字体仅"涉及估值"的交叉验证出图才用到——**纯批注+审核意见两件套零第三方依赖**。
- **跨平台传数据用 JSON 配置 + Write 工具，别用 heredoc / `python -c` 塞中文**：在 Windows Git Bash 等环境，中文路径/内容经 heredoc 易乱码致脚本失败。正确做法：用 Write 工具写 UTF-8 的 `配置.json`，再 `python build_xxx.py 配置.json`（脚本已 `reconfigure utf-8`，打印不崩）。
- **批注锚点**：用报告中"能唯一定位的一段连续原文"；脚本已自动去空白+反转义，但仍避免横跨多段或含可能被改写的字符；若打印"未找到锚点"，照它给的候选段落换更短的连续原文。
- **老式 OLE2 二进制 .doc 读不出中文**（antiword 无 CJK、UTF-16 多为噪声）→ 请报告方**另存为 .docx** 再审。
- **扫描件 PDF（无文字层）读不了**→ 需 OCR 或请对方提供可读版/口述关键信息（如不动产证：签发日期、权利人、用途、面积、年限）。
- **务必先核测算表再对问题定性**——勿仅据报告正文文字下"重大"结论（正文可能是笔误/模板残留，实际测算才是准）。
- 严格一致的前提：**确实套用本包模板 + 跑本包脚本**；若 AI 仅自由撰写，只能格式相近。
- **性能与默认取向（默认＝平衡，省 token / 提速）**：①只读 `SKILL.md` + **本次报告类型对应的那一个** `rules/*.md`（已是依据的提炼），**不要通读全部 rules、更不要复读桌面原始依据**（准则汇编/法规汇编/案例汇编等动辄数十 MB，全读一次即烧大量 token 且慢）；②确需某条款时，再**按需、分段**（offset/limit）打开对应依据原文，不整本读；③长中文文件先一次性抽成 .txt 再读，避免反复解析 docx/xlsx；④**先核测算表/明细表数值，再对问题定性**；⑤高风险/对外重大项目可临时切换"完整"取向（回查依据原文逐条印证）。

## 六、维护与版本
规则变了：改 `rules/` 对应 .md；格式/模板变了：换 `templates/` docx 并同步改 `scripts/`。建议整个文件夹纳入 **git** 版本控制，留痕、可回退、可同步给团队。

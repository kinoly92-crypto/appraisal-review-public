# 换平台 / 给同事用 · 一页核对清单（打印或贴桌面）

> 目的：在**任何能"读写文件+运行 Python"的 AI**上，产出与原作者**格式一致**的三件套。纯聊天、不能跑代码的 AI 做不到一致。

## ① 环境（一次性·装一次别重复）
- [ ] 装 **Python 3**；开会话先跑 **`python scripts/check_env.py`** 自检
- [ ] 依赖**缺了才装、只装一次**（会话内持久）：`pip install -r scripts/requirements.txt`（`openpyxl`/`matplotlib` 均为**可选增强**——缺 openpyxl 自动纯标准库读 .xlsx；matplotlib 仅交叉验证出图用）
- [ ] 中文字体 **SimHei**（Win 自带；Mac/Linux 缺则装一个，脚本会自动兜底找 CJK 字体，或在 JSON 配置给 `FONT`）
- [ ] AI 助手能**读写本地文件 + 运行 Python**
- [ ] 把**整个技能包文件夹**给 AI（含 `SKILL.md`/`rules`/`templates`/`scripts`）

## ② 开场提示词（复制发给 AI，见 `GUIDE-for-colleague.md` 第三节）
关键四句：先读 `SKILL.md`+对应 `rules/` → **先用文字问"审核人/批注人/审核日期"并停下等我回答** → 先核测算表再定性 → 严格套 `templates/`+`scripts/` 出三件套、不另起格式。

## ③ 流程（每次）
1. [ ] AI **用普通文字**问：审核人姓名、批注人姓名、审核日期（默认今天）→ **等你回答再继续**（不依赖弹窗/表单；没答不读资料、不出成果）
2. [ ] 判断报告类型 → 读 `SKILL.md` + 对应**一个** `rules/*.md`（资产/房地产/土地/债权/矿业权/其他咨询类；司法处置叠加 `judicial-disposal.md`）
3. [ ] 读资料：报告+说明(资产评估才有)+测算表+权属证+合同。老 `.doc` 读不出中文→转 `.docx`；扫描 PDF 无文字层→提供可读版或口述关键信息
4. [ ] **先核测算表/明细表数值，再对问题定性**（别只看正文文字下"重大"结论）
5. [ ] 涉及估值的→公开数据交叉验证（搜不到如实说、不编造）
6. [ ] 出三件套（见④）

## ④ 三件套（形式必须一致）
- [ ] **审核意见**（套 `opinion-template.docx`）：逐条、Word**自动连续编号**、按文档大类分组、每大类首条前加类别提示行
- [ ] **批注版报告**（跑 `build_annotated`）：Word**真批注**、**直接描述问题不加[重大]等前缀**、作者=批注人姓名（资产评估对报告+说明各出一份）
- [ ] **交叉验证**（套 `crossvalidation-template.docx`）：数据对比表+两图+**来源做成可点击超链接**
- [ ] ⚠ **不涉及估值的咨询报告（无价值数额结论）：不做交叉验证，只出 批注版+审核意见 两件套**

## ⑤ 交付前自检
- [ ] 审核意见编号连续、信息表**审核人/项目号/日期**正确
- [ ] 无其他项目残留（他公司名、错基准日、`#REF!`、串户数据）
- [ ] 三件套能在 Word 正常打开；交叉验证链接可点击
- [ ] 审核意见与交叉验证**字体仿宋_GB2312、行距1.5倍、段前段后0**（套本包模板即自动一致）

## ⑥ 省 token / 提速（默认＝平衡）
- 只读 `SKILL.md`+**对应一个** `rules/*.md`；**不通读全部 rules、不复读桌面原始大依据**
- 需要某条款再**分段**打开对应依据原文，不整本读；长中文文件先抽 `.txt`

## ⑦ 常见坑（含提速）
- **依赖每次重装拖慢** → 先 `check_env.py`，依赖会话内持久，**只装一次**；读 .xlsx 不必装 openpyxl（已内置降级）
- **中文 heredoc 乱码致脚本失败**（Win Git Bash）→ 出三件套**用 Write 工具写 UTF-8 `配置.json` + `python build_xxx.py 配置.json`**，别把中文塞进 heredoc / `python -c`
- **批注锚点找不到** → 已自动去空白+反转义；仍失配就照脚本打印的候选段落，换更短的连续原文，勿横跨多段
- 老 `.doc`/扫描 PDF 读不了 → 让对方转 `.docx`/提供可读版
- 中文路径在某些环境乱码 → 用文件名关键词定位、必要时复制成英文名再处理
- 规则/格式要改 → 改 `rules/*.md`（规则）或 `templates/*.docx`（格式），**联系原作者统一维护**，整包重发（建议 git）
